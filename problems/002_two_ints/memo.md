# 問題メモ

環境の動作確認用に同梱しているサンプル（paiza の問題ではありません）。

- 制限: 5.0 秒 / 512 MB

## 入力

1 行に空白区切りで 2 つの整数。

```
3 5
```

## 出力

2 つの合計。

```
8
```

## 気づいたこと

`Console.ReadLine().Split(' ')` で分割して `int.Parse` する。
要素数が可変なら `Array.ConvertAll(..., int.Parse)` が短い。
